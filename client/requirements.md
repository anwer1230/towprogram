## Packages
framer-motion | Page transitions and scroll-triggered animations
lucide-react | Standard icons (ensuring it's listed for the premium look)
date-fns | Formatting log timestamps

## Notes
- WebSocket connects to standard host (e.g. `ws://${window.location.host}`)
- WhatsApp QR codes are provided as base64 data URLs via WebSocket, no extra QR library needed
- Minimal clean design style relies heavily on spacing, shadows, and crisp typography
